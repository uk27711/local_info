<!DOCTYPE html>
<html lang="ja">

  <div class="container" style="margin-top: 60px;">
    <div class="row justify-content-center">
      <div class="col-md-4"> <!-- 画像の左側部分 -->

        <% if @post.image.attached? %>
          <div class="form-group">
            <label>Current Image</label>
            <%= image_tag @post.image, size: "200x250", class: "img-fluid" %> <!-- 画像を表示 -->
            <br>
            <!-- 画像削除用のボタン -->
            <%= link_to 'Delete Image', delete_image_post_path(@post), method: :delete, data: { confirm: 'Are you sure you want to delete this image?' }, class: "btn btn-danger btn-sm mt-2" %>
          </div>
        <% end %>
      </div>

      <div class="col-md-6"> <!-- フォーム部分 -->
        <%= form_with(model: @post, url: post_path(@post), local: true, authenticity_token: true, method: :patch, html: { multipart: true }) do |f| %>
          
          <!-- 画像アップロードフォームを画像の下に配置 -->
          <div class="form-group">
            <label for="post_image">Upload a new image</label>
            <%= f.file_field :image %> <!-- 新しい画像をアップロードするフォーム -->
          </div>

          <div class="form-group">
            <label for="post_title">Title</label>
            <%= f.text_field :title, class: "form-control" %>
          </div>

          <div class="form-group">
            <label for="post_body">Opinion</label>
            <%= f.text_area :body, class: "form-control" %>
          </div>

          <div class="form-group">
            <%= button_to 'Update', post_path(@post), method: :patch, class: "btn btn-primary btn-sm" %>
            <%= link_to 'Show', post_path(@post), class: "text-secondary ml-3" %> <!-- "Show"リンク -->
            <%= link_to 'Back', posts_path, class: "text-secondary ml-3" %> <!-- "Back"リンク -->
          </div>

          <% if @post.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@post.errors.count, "error") %> prohibited this @post from being saved:</h2>
              <ul>
                <% @post.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</html>
